<UserControl x:Class="ImageViewer.Control.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageViewer.Control"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Row="0" Grid.RowSpan="2" Grid.Column="0"
                 Text="{Binding Value, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:NumericUpDown}}}"
                 HorizontalContentAlignment="Right" VerticalContentAlignment="Center"
                 MouseWheel="TextBox_MouseWheel" />
        <RepeatButton Grid.Row="0" Grid.Column="1" Content="▲" Click="UpButton_Click" />
        <RepeatButton Grid.Row="1" Grid.Column="1" Content="▼" Click="DownButton_Click" />
    </Grid>
</UserControl>
